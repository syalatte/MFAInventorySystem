@model IEnumerable<MFAInventorySystem.Models.tb_stockhistory>

@{
    ViewBag.Title = "Index";
    Layout = "~/Views/Shared/_Layout.cshtml";
}


<!-- Begin Page Content -->
<div class="container-fluid">
    @if (TempData["AlertMessage"] != null)
    {
        <div class="alert alert-success">
            @TempData["AlertMessage"]
        </div>

    }
    <!-- Page Heading -->
    <!-- DataTales Example -->
<div class="card shadow mb-4">
    <div class="card-header py-3">
        <h4 class="m-0 font-weight-bold text-primary" style="float:left;">Manage Stocks Out</h4>
        <!--@Html.ActionLink("Print", "PrintAll", null, new { @class = "btn btn-outline-secondary", style = "float:right;" }) -->

    </div>
    <div>
        <input type="button" value="Print" onclick="JavaScript:printPartOfPage('printArea');" />
    </div>
    <div class="card-body">
        <div class="table-responsive" id="printArea">
            <p>
                @Html.ActionLink("Create New", "Create", null, new { @class = "btn btn-primary" })
                @Html.ActionLink("View All", "Index", null, new { @class = "btn btn-primary" })
            </p>
            @using (Html.BeginForm("Index", "StockHistory", FormMethod.Get))
            {
                <fieldset>
                    <legend>Search criteria</legend>
                    @Html.Label("startdate", "Start Date:")
                    <input class="startdate" id="startdate" name="startdate" type="date" value="">
                    @Html.Label("enddate", "End Date:")
                    <input class="enddate" id="enddate" name="enddate" type="date" value="">
                    <input type="submit" value="Apply" />
                </fieldset>
            }

            <table id="example1" class="table table-striped table-bordered table-hover table-sm" cellspacing="0" width="100%">
                <thead>
                    <tr>
                        <th>
                            @Html.DisplayNameFor(model => model.sh_qtySlot)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.sh_untungBersih)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.sh_date)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.sh_qtySold)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tb_stock.s_product)
                        </th>
                        <th>
                            @Html.DisplayNameFor(model => model.tb_vendingmachine.v_location)
                        </th>
                        <th></th>
                    </tr>
                </thead>
                <tbody>
                    @foreach (var item in Model)
                    {
                        <tr>
                            <td>
                                @Html.DisplayFor(modelItem => item.sh_qtySlot)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.sh_untungBersih)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.sh_date)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.sh_qtySold)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tb_stock.s_product)
                            </td>
                            <td>
                                @Html.DisplayFor(modelItem => item.tb_vendingmachine.v_location)
                            </td>
                            <td>
                                @Html.ActionLink("Edit", "Edit", new { id = item.sh_id }, new { @class = "btn btn-dark btnStyle" })
                                @Html.ActionLink("Details", "Details", new { id = item.sh_id }, new { @class = "btn btn-secondary btnStyle" })
                                @Html.ActionLink("Delete", "Delete", new { id = item.sh_id }, new { @class = "btn btn-danger btnStyle" })
                            </td>
                        </tr>
                    }
                </tbody>
            </table>
        </div>
    </div>
</div>
</div>

<script type="text/javascript">
<!--
function printPartOfPage(elementId) {
var printContent = document.getElementById(elementId);
var windowUrl = 'Repert Details';
var uniqueName = new Date();
var windowName = 'Print' + uniqueName.getTime();
var printWindow = window.open(windowUrl, windowName, 'left=50000,top=50000,width=0,height=0');
printWindow.document.write(printContent.innerHTML);
printWindow.document.close();
printWindow.focus();
printWindow.print();
printWindow.close();
}
// -->
</script>